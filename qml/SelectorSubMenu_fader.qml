import QtQuick 1.1;import com.nokia.symbian 1.1;Page{QueryDialog{privateCloseIcon:true;titleText:qsTr("Popupfader");message:qsTr("Disable/Enable the popup fader?\n\nThat's the black mask which overlays the UI after opening a popup or submenu.\nBy default it's disabled in Delight.");acceptButtonText:qsTr("Enable");rejectButtonText:qsTr("Disable");onButtonClicked:{if(index==0)Helper.resolver("avkon_enable.txt");else Helper.resolver("avkon_disable.txt");}onStatusChanged:if(status===DialogStatus.Closing)destroy();Component.onCompleted:open();}}