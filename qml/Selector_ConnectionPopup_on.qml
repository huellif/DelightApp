import QtQuick 1.1;import com.nokia.symbian 1.1;QueryDialog{titleText:qsTr("Connection popup");icon:"images/note_info.svg";message:qsTr("This is a popup that notifies immediately which bearer (either 3G or WLAN) has initiated the internet connection. Do you want to restore it back?\n\nWARNING: your phone will reboot after changing this.");acceptButtonText:"OK";rejectButtonText:qsTr("Cancel");onAccepted:{Helper.kill(0x101F6D12);Helper.resolver("ConnectionPopup_on.txt");}Component.onCompleted:open();onStatusChanged:if(status===DialogStatus.Closing)destroy();}