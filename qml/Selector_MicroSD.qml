import QtQuick 1.1;import com.nokia.symbian 1.1;Page{QueryDialog{privateCloseIcon:true;titleText:qsTr("MicroSD card in notification bar");message:qsTr("Do you want to disable / enable microSD card icon (when inserted on your device) in the status bar?\n\nBy default it is enabled in Delight.");acceptButtonText:qsTr("Enable");rejectButtonText:qsTr("Disable");onButtonClicked:{if(index==0)Helper.resolver("MicroSD_on.txt");else Helper.resolver("MicroSD_off.txt");}onStatusChanged:if(status===DialogStatus.Closing)destroy();Component.onCompleted:open();}}