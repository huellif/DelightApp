import QtQuick 1.1;import com.nokia.symbian 1.1;QueryDialog{titleText:qsTr("Application caching");icon:"images/note_info.svg";message:qsTr("You cannot close: Clock, Calendar, Logs, Contacts and Messaging as in original firmware. This means these applications are launching instantly, but they will consume more battery, CPU time and free RAM.\n\nApplying this requires a reboot.");acceptButtonText:"OK";rejectButtonText:qsTr("Cancel");onAccepted:Helper.resolver("Cache_on.txt");Component.onCompleted:open();onStatusChanged:if(status===DialogStatus.Closing)destroy();}