import QtQuick 1.1;import com.nokia.symbian 1.1;QueryDialog{titleText:qsTr("Application caching");icon:"images/note_info.svg";message:qsTr("Close all system apps except contacts and messaging?\nYou can open these two faster, but it needs more RAM.\nYou can close: clock, calendar, logs");acceptButtonText:qsTr("OK");rejectButtonText:qsTr("Cancel");onAccepted:Helper.resolver("cache_some.txt");Component.onCompleted:open();onStatusChanged:if(status===DialogStatus.Closing)destroy();}