import QtQuick 1.1;import com.nokia.symbian 1.1;QueryDialog{titleText:qsTr("HS and widgets");icon:"images/note_info.svg";message:qsTr("This will reset homescreen and add new widgets (if you flashed via Update Software).");acceptButtonText:qsTr("Fix");rejectButtonText:qsTr("Cancel");onAccepted:Helper.resolver("HS_widgets.txt");onStatusChanged:if(status===DialogStatus.Closing)destroy();Component.onCompleted:open();}