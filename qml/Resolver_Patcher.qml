import QtQuick 1.1;import com.nokia.symbian 1.1;QueryDialog{titleText:qsTr("ROMPatcher+ fixer");icon:"images/note_info.svg";message:qsTr("Cannot launch ROMPatchter by Il.Socio anymore (e.g. after a system crash)?\n\nWARNING: this will reset your ROMPatcher autostart configuration.");acceptButtonText:qsTr("Fix");rejectButtonText:qsTr("Cancel");onAccepted:Helper.resolver("Patcher.txt");onStatusChanged:if(status===DialogStatus.Closing)destroy();Component.onCompleted:open();}