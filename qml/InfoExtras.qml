import QtQuick 1.1;import com.nokia.symbian 1.1;QueryDialog{titleText:qsTr("Extras");icon:"images/note_info.svg";message:qsTr("Extras is an easy wizard to change multiple things.\n\nJust touch few options and read some information, you always can cancel before the scripts run. Some scripts need a restart.\n\nExtras is split into multiple sub-applications:\n- Classic Resolver, which helps to solve typical (custom) firmware problems.\n- My Own MiniCMD, which provides options to launch MiniCMD scripts and the MiniCMD guide.\n- Reboot the Phone, which does what the title says.\n- Selector, which lets you choose between diffrent settings.\n- Tools, which brings you even more advanced tweaks and utilities.");acceptButtonText:qsTr("Back");onAccepted:destroy();Component.onCompleted:open();}