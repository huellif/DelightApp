import QtQuick 1.1;import com.nokia.symbian 1.1;QueryDialog{titleText:qsTr("Chat fixer");icon:"images/note_info.svg";message:qsTr("Some chat applications (like Lightbulb) show a chat icon in the notification bar if you have unread messages.\n\nIf these programs get closed or crash, it still could be enabled. Do you want to disable / remove it?");acceptButtonText:qsTr("Fix");rejectButtonText:qsTr("Cancel");onAccepted:Helper.disableChatIcon();onStatusChanged:if(status===DialogStatus.Closing)destroy();Component.onCompleted:open();}