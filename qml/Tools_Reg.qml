import QtQuick 1.1;import com.nokia.symbian 1.1;QueryDialog{titleText:qsTr("SISRegistry B&R");icon:"images/note_info.svg";message:qsTr("This tool lets you backup / restore SISRegistry.\n\nFor some minutes your phone will be slowed down due to backup process! Do make sure that you have enabled RemoveHashCheck patch in ROMPatcher. If not, cancel and enable the patch first.\n\nDo you want to launch it?");acceptButtonText:qsTr("Backup/Restore");rejectButtonText:qsTr("Cancel");onAccepted:{Helper.resolver("DReg.del");destroy();}onRejected:destroy();Component.onCompleted:open();}