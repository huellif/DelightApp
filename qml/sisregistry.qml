import QtQuick 1.1;import com.nokia.symbian 1.1;QueryDialog{titleText:qsTr("Registry Fixer");icon:"images/note_info.svg";message:qsTr("Do you want to refresh your sisregistry?\n\nThis will reset the list of ROM apps and apply manual registry modification.\nYour phone will reboot at the end!\nAfter your phone rebooted it has to recreate some files, you can't install .sis(x) files during this process.");acceptButtonText:qsTr("Fix");rejectButtonText:qsTr("Cancel");onAccepted:Helper.resolver("sisregistry.txt");onStatusChanged:if(status===DialogStatus.Closing)destroy();Component.onCompleted:open();}